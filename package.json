{
  "name": "SeparateRNPackage",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "react-native start",
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "test": "jest",
    "build:ios": "react-native bundle --entry-file='index.js' --bundle-output='./ios/bundle/main.jsbundle' --dev=false --platform='ios'"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-native": "^0.72.10"
  },
  "devDependencies": {
    "react-native-bundle-visualizer": "^3.1.3"
  }
}
